apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hanami-project.fullname" . }}-test-connection"
  labels:
    {{- include "hanami-project.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hanami-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
