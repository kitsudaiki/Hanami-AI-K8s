apiVersion: v1
kind: Service
metadata:
  name: {{ include "hanami-project.fullname" . }}
  labels:
    {{- include "hanami-project.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hanami-project.selectorLabels" . | nindent 4 }}
